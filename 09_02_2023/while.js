var nMastigadas = 20;
while(nMastigadas >= 1) {
    console.log(` Mastigada de número ${nMastigadas} realizada`);
    nMastigadas--;
}
console.log('Finalmente engoliu');

// 1. Uma empresa de tecnologia contratou você para realizar um pequeno script, que exibirá, na tela, 
// uma contagem regressiva de 10 até 0.

// 2. Construir um programa no qual o 
// usuário digitará um número e o programa fará a
// tabuada de multiplicação deste número.

// 3. Construir um programa no qual o usuário
// digitará um número e o programa fará a 
// contagem regressiva deste número, até o número zero.